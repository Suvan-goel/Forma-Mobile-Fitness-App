diff --git a/node_modules/react-native-record-screen/android/src/main/java/com/recordscreen/RecordScreenModule.kt b/node_modules/react-native-record-screen/android/src/main/java/com/recordscreen/RecordScreenModule.kt
index e3850d5..67a1ceb 100644
--- a/node_modules/react-native-record-screen/android/src/main/java/com/recordscreen/RecordScreenModule.kt
+++ b/node_modules/react-native-record-screen/android/src/main/java/com/recordscreen/RecordScreenModule.kt
@@ -46,16 +46,14 @@ class RecordScreenModule(reactContext: ReactApplicationContext) : ReactContextBa
 
   private val mActivityEventListener: ActivityEventListener = object : BaseActivityEventListener() {
     override fun onActivityResult(activity: Activity, requestCode: Int, resultCode: Int, intent: Intent?) {
-      if (requestCode == SCREEN_RECORD_REQUEST_CODE) {
-        if (resultCode == AppCompatActivity.RESULT_OK) {
-          hbRecorder!!.startScreenRecording(intent, resultCode);
-        } else {
-          startPromise!!.resolve("permission_error");
-        }
+      if (requestCode != SCREEN_RECORD_REQUEST_CODE) return;
+      if (resultCode == AppCompatActivity.RESULT_OK) {
+        hbRecorder?.startScreenRecording(intent, resultCode);
+        startPromise?.resolve("started");
       } else {
-        startPromise!!.reject("404", "cancel!");
+        startPromise?.resolve("permission_error");
       }
-      startPromise!!.resolve("started");
+      startPromise = null;
     }
   }
 
@@ -124,7 +122,17 @@ class RecordScreenModule(reactContext: ReactApplicationContext) : ReactContextBa
   @ReactMethod
   fun stopRecording(promise: Promise) {
     stopPromise = promise
-    hbRecorder!!.stopScreenRecording();
+    if (hbRecorder != null) {
+      hbRecorder!!.stopScreenRecording();
+    } else {
+      val response = WritableNativeMap();
+      val result = WritableNativeMap();
+      result.putString("error", "Recorder not initialized");
+      response.putString("status", "error");
+      response.putMap("result", result);
+      promise.resolve(response);
+      stopPromise = null;
+    }
   }
 
   @ReactMethod
@@ -149,15 +157,25 @@ class RecordScreenModule(reactContext: ReactApplicationContext) : ReactContextBa
       response.putString("status", "success");
       response.putMap("result", result);
       stopPromise!!.resolve(response);
+      stopPromise = null;
     }
   }
 
   override fun HBRecorderOnError(errorCode: Int, reason: String?) {
-    println("HBRecorderOnError")
-    println("errorCode")
-    println(errorCode)
-    println("reason")
-    println(reason)
+    println("HBRecorderOnError: errorCode=$errorCode, reason=$reason")
+    if (stopPromise != null) {
+      val response = WritableNativeMap();
+      val result = WritableNativeMap();
+      result.putString("error", reason ?: "Recording error (code: $errorCode)");
+      response.putString("status", "error");
+      response.putMap("result", result);
+      stopPromise!!.resolve(response);
+      stopPromise = null;
+    }
+    if (startPromise != null) {
+      startPromise!!.resolve("permission_error");
+      startPromise = null;
+    }
   }
 
   override fun HBRecorderOnPause() {
diff --git a/node_modules/react-native-record-screen/ios/RecordScreen.mm b/node_modules/react-native-record-screen/ios/RecordScreen.mm
index 67ac51f..8daddef 100644
--- a/node_modules/react-native-record-screen/ios/RecordScreen.mm
+++ b/node_modules/react-native-record-screen/ios/RecordScreen.mm
@@ -4,6 +4,7 @@
 @implementation RecordScreen
 
 UIBackgroundTaskIdentifier _backgroundRenderingID;
+dispatch_queue_t _writingQueue;
 
 - (NSDictionary *)errorResponse:(NSDictionary *)result;
 {
@@ -86,7 +87,8 @@ - (int) adjustMultipleOf2:(int)value;
     self.writer = [AVAssetWriter assetWriterWithURL:[NSURL fileURLWithPath:videoOutPath] fileType:AVFileTypeMPEG4 error:&error];
     if (!self.writer) {
         NSLog(@"writer: %@", error);
-        abort();
+        resolve([self errorResponse:@{@"error": error.localizedDescription ?: @"Failed to create asset writer"}]);
+        return;
     }
 
     AudioChannelLayout acl = { 0 };
@@ -123,6 +125,8 @@ - (int) adjustMultipleOf2:(int)value;
     [self.writer setMovieTimeScale:60];
     [self.videoInput setExpectsMediaDataInRealTime:YES];
 
+    _writingQueue = dispatch_queue_create("com.recordscreen.writing", DISPATCH_QUEUE_SERIAL);
+
     if (self.enableMic) {
         self.screenRecorder.microphoneEnabled = YES;
     }
@@ -133,7 +137,7 @@ - (int) adjustMultipleOf2:(int)value;
                 [self.screenRecorder startCaptureWithHandler:^(CMSampleBufferRef sampleBuffer, RPSampleBufferType bufferType, NSError* error) {
                     // Failure to do so will result in a memory error when accessing the sampleBuffer in the main thread.
                     CFRetain(sampleBuffer);
-                    dispatch_async(dispatch_get_main_queue(), ^{
+                    dispatch_async(_writingQueue, ^{
                         if (CMSampleBufferDataIsReady(sampleBuffer)) {
                             if (self.writer.status == AVAssetWriterStatusUnknown && !self.encounteredFirstBuffer && bufferType == RPSampleBufferTypeVideo) {
                                 self.encounteredFirstBuffer = YES;
@@ -252,11 +256,44 @@ - (int) adjustMultipleOf2:(int)value;
 
     dispatch_async(dispatch_get_main_queue(), ^{
         if (@available(iOS 11.0, *)) {
+            if (!self.screenRecorder || ![RPScreenRecorder sharedRecorder].isRecording) {
+                // Not recording — resolve immediately to prevent hanging
+                NSLog(@"stopRecording: not currently recording, resolving immediately");
+                resolve([self errorResponse:@{@"error": @"Not currently recording"}]);
+                return;
+            }
             [[RPScreenRecorder sharedRecorder] stopCaptureWithHandler:^(NSError * _Nullable error) {
-                if (!error) {
+                if (error) {
+                    // Handle error — resolve with error info instead of hanging
+                    NSLog(@"stopRecording error: %@", error);
+                    resolve([self errorResponse:@{@"error": error.localizedDescription ?: @"Unknown stop error"}]);
+                    self.audioInput = nil;
+                    self.micInput = nil;
+                    self.videoInput = nil;
+                    self.writer = nil;
+                    self.screenRecorder = nil;
+                    return;
+                }
+
+                // Dispatch to the writing queue so any in-flight buffer writes
+                // finish before we finalize the asset writer.
+                dispatch_queue_t queue = _writingQueue ?: dispatch_get_main_queue();
+                dispatch_async(queue, ^{
                     [self.audioInput markAsFinished];
                     [self.micInput markAsFinished];
                     [self.videoInput markAsFinished];
+
+                    if (!self.writer) {
+                        // Writer was never created — resolve with error instead of hanging
+                        NSLog(@"stopRecording: writer is nil, cannot finish writing");
+                        resolve([self errorResponse:@{@"error": @"Writer not initialized"}]);
+                        self.audioInput = nil;
+                        self.micInput = nil;
+                        self.videoInput = nil;
+                        self.screenRecorder = nil;
+                        return;
+                    }
+
                     [self.writer finishWritingWithCompletionHandler:^{
 
                         NSDictionary *result = [NSDictionary dictionaryWithObject:self.writer.outputURL.absoluteString forKey:@"outputURL"];
@@ -269,10 +306,11 @@ - (int) adjustMultipleOf2:(int)value;
                         self.writer = nil;
                         self.screenRecorder = nil;
                     }];
-                }
+                });
             }];
         } else {
             // Fallback on earlier versions
+            resolve([self errorResponse:@{@"error": @"iOS 11+ required"}]);
         }
     });
 }
